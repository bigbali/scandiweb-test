import React, { PureComponent } from 'react'

export default class CartPage extends PureComponent {
    render() {
        return (
            <main>
                CartPage
            </main>
        )
    }
}
